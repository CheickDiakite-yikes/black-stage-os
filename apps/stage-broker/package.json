{
  "name": "@blackstage/stage-broker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm build && node dist/server.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test test/stageBrokerServer.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@blackstage/voice-core": "workspace:*"
  }
}
