{
  "name": "@blackstage/voice-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test test/realtimeVoiceSession.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
